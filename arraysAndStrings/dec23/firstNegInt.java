public class firstNegInt {
    static int firstNegative(int[] arr, int k) {
        int n = arr.length;
        if (n < k) {
            return -1;
        }

        int windowSum = 0;
        for (int i = 0; i < k; i++) {
            
        }
        return 1;
    }
}
